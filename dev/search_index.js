var documenterSearchIndex = {"docs":
[{"location":"guide/#Guide","page":"Guide","title":"Guide","text":"","category":"section"},{"location":"guide/#Basic-Usage","page":"Guide","title":"Basic Usage","text":"Create a source, get a token, pass it to operations:\n\nusing CancellationTokens\n\nsrc = CancellationTokenSource()\ntoken = get_token(src)\n\n# Later, from another task or thread:\ncancel(src)\n\nCheck whether cancellation has been requested without blocking:\n\nif is_cancellation_requested(token)\n    # clean up\nend\n\nOr block until cancellation occurs:\n\nwait(token)  # returns when cancel(src) is called","category":"section"},{"location":"guide/#Timeout-Based-Cancellation","page":"Guide","title":"Timeout-Based Cancellation","text":"A source can auto-cancel after a given number of seconds:\n\nsrc = CancellationTokenSource(5.0)   # cancels after 5 seconds\nwait(get_token(src))                 # blocks for ~5 s\n\nThis is useful for implementing timeouts on operations that accept a token:\n\n# Give the operation at most 10 seconds\nsrc = CancellationTokenSource(10.0)\nresult = take!(channel, get_token(src))","category":"section"},{"location":"guide/#Combined-/-Linked-Sources","page":"Guide","title":"Combined / Linked Sources","text":"A combined source cancels when any of its parent tokens is cancelled:\n\ntimeout_src = CancellationTokenSource(30.0)\nmanual_src  = CancellationTokenSource()\n\ncombined = CancellationTokenSource(get_token(timeout_src), get_token(manual_src))\ntoken = get_token(combined)\n\n# `token` will be cancelled if the timeout expires OR if manual_src is cancelled\nsleep(60.0, token)\n\nThe parent sources are not affected when the combined source is cancelled through one of them — cancellation only flows downward.","category":"section"},{"location":"guide/#Handling-OperationCanceledException","page":"Guide","title":"Handling OperationCanceledException","text":"Cancellable functions throw OperationCanceledException when their token is cancelled. Catch it to perform cleanup:\n\ntry\n    data = take!(channel, token)\n    process(data)\ncatch ex\n    if ex isa OperationCanceledException\n        println(\"Operation was cancelled\")\n        # The channel is still usable after cancellation\n    else\n        rethrow()\n    end\nend\n\nRetrieve the token that caused the exception with get_token:\n\ncatch ex::OperationCanceledException\n    tok = get_token(ex)\n    @info \"Cancelled\" is_cancellation_requested(tok)\nend","category":"section"},{"location":"guide/#Building-Custom-Cancellable-Operations","page":"Guide","title":"Building Custom Cancellable Operations","text":"Use wait(token) and is_cancellation_requested(token) to add cancellation support to your own functions:\n\nfunction my_long_computation(token::CancellationToken)\n    result = 0\n    for i in 1:1_000_000\n        # Periodically check for cancellation\n        if i % 1000 == 0\n            is_cancellation_requested(token) && throw(OperationCanceledException(token))\n        end\n        result += expensive_step(i)\n    end\n    return result\nend\n\nFor operations that block on a condition variable or event, spawn a monitoring task that wakes up the waiter on cancellation:\n\nfunction my_blocking_op(token::CancellationToken)\n    is_cancellation_requested(token) && throw(OperationCanceledException(token))\n\n    done = Threads.Event()\n    result = Ref{Any}(nothing)\n\n    # Worker\n    worker = @async begin\n        result[] = do_work()\n        notify(done)\n    end\n\n    # Cancellation monitor\n    monitor = @async begin\n        wait(token)\n        notify(done)\n    end\n\n    wait(done)\n\n    if is_cancellation_requested(token)\n        throw(OperationCanceledException(token))\n    end\n    return result[]\nend","category":"section"},{"location":"guide/#Callback-Registration","page":"Guide","title":"Callback Registration","text":"Instead of spawning a task to watch for cancellation, you can register a callback that is invoked synchronously when cancel is called — matching .NET's CancellationToken.Register(Action):\n\nsrc = CancellationTokenSource()\ntoken = get_token(src)\n\nreg = register(token) do\n    println(\"Cancelled!\")\nend\n\ncancel(src)   # prints \"Cancelled!\" immediately, inline with cancel()\n\nCallbacks are invoked synchronously during cancel(), so they should be non-blocking and fast.\n\nIf the token is already cancelled when register is called, the callback is invoked immediately before register returns.","category":"section"},{"location":"guide/#Deregistration","page":"Guide","title":"Deregistration","text":"register returns a CancellationTokenRegistration handle.  Call close on it to prevent the callback from being invoked:\n\nsrc = CancellationTokenSource()\nreg = register(get_token(src)) do\n    error(\"should not run\")\nend\n\nclose(reg)     # deregister\ncancel(src)    # callback is NOT invoked\n\nDeregistration is idempotent — calling close multiple times is safe.","category":"section"},{"location":"guide/#Use-Cases","page":"Guide","title":"Use Cases","text":"Callback registration is useful when you need to perform a side effect on cancellation without spawning a monitoring task:\n\n# Close a socket when cancelled\nreg = register(token) do\n    close(socket)\nend\n\ntry\n    process(socket)\nfinally\n    close(reg)   # clean up registration if we finish normally\nend\n\nIt is also how combined sources (CancellationTokenSource(token1, token2, ...)) are implemented internally — each parent token registers a callback instead of spawning a monitoring task.","category":"section"},{"location":"guide/#Socket-readline-Cancellation","page":"Guide","title":"Socket readline Cancellation","text":"readline(socket, token) supports cancellation on TCPSocket and PipeEndpoint.  When the token is cancelled, the socket is closed to unblock the pending read:\n\nsrc = CancellationTokenSource(5.0)\ntry\n    line = readline(socket, get_token(src))\n    process(line)\ncatch ex\n    if ex isa OperationCanceledException\n        @info \"Read timed out — socket has been closed\"\n        # Reconnect if needed\n    else\n        rethrow()\n    end\nend\n\nnote: Why the socket is closed\nClosing the socket is the only safe way to interrupt a blocking read without corrupting other tasks.  The previous approach injected an error into the socket's shared condition variable, which would crash all tasks waiting on the same socket — not just the one that requested cancellation.  Closing the socket gives every reader a clean I/O error instead.For typical timeout use cases this is the right behaviour: if a read timed out, the protocol state is usually indeterminate and the connection should be re-established.","category":"section"},{"location":"guide/#Socket-read-Cancellation","page":"Guide","title":"Socket read Cancellation","text":"read(socket, nb, token) supports cancellation on TCPSocket and PipeEndpoint, reading a fixed number of bytes.  Like readline, the socket is closed when the token is cancelled:\n\nsrc = CancellationTokenSource(5.0)\ntry\n    data = read(socket, 1024, get_token(src))\n    process(data)\ncatch ex\n    if ex isa OperationCanceledException\n        @info \"Read timed out — socket has been closed\"\n        # Reconnect if needed\n    else\n        rethrow()\n    end\nend\n\nThe same rationale applies: closing the socket is the only safe way to interrupt a blocking read without corrupting other tasks on the same socket.","category":"section"},{"location":"guide/#Resource-Cleanup","page":"Guide","title":"Resource Cleanup","text":"CancellationTokenSource implements close, which is equivalent to cancel. This enables do-block patterns for scoped cancellation:\n\nsrc = CancellationTokenSource()\ntry\n    run_operation(get_token(src))\nfinally\n    close(src)  # ensures timer and callbacks are cleaned up\nend","category":"section"},{"location":"api/#API-Reference","page":"API Reference","title":"API Reference","text":"","category":"section"},{"location":"api/#Types","page":"API Reference","title":"Types","text":"","category":"section"},{"location":"api/#Functions","page":"API Reference","title":"Functions","text":"","category":"section"},{"location":"api/#CancellationTokens.CancellationTokenSource","page":"API Reference","title":"CancellationTokens.CancellationTokenSource","text":"CancellationTokenSource()\nCancellationTokenSource(seconds::Real)\nCancellationTokenSource(tokens::CancellationToken...)\n\nA source of cancellation signals. Create one, hand out tokens with get_token, and call cancel when the operation should stop.\n\nConstructors\n\nCancellationTokenSource() — a manually-cancelled source.\nCancellationTokenSource(seconds) — auto-cancels after seconds.\nCancellationTokenSource(token1, token2, ...) — cancels when any parent token is cancelled (linked / combined source).\n\nExamples\n\nsrc = CancellationTokenSource()\ntoken = get_token(src)\n\n@async begin\n    sleep(1.0)\n    cancel(src)\nend\n\nwait(token)  # blocks until cancel is called\n\n# Auto-cancel after 5 seconds\nsrc = CancellationTokenSource(5.0)\n\n# Cancel when either parent fires\ncombined = CancellationTokenSource(get_token(src1), get_token(src2))\n\n\n\n\n\n","category":"type"},{"location":"api/#CancellationTokens.CancellationToken","page":"API Reference","title":"CancellationTokens.CancellationToken","text":"CancellationToken\n\nA lightweight, immutable handle obtained from a CancellationTokenSource via get_token.  Pass it to cancellable operations as the last positional argument.\n\nTokens are cheap to copy and safe to share across tasks and threads.\n\nSee also is_cancellation_requested, wait.\n\n\n\n\n\n","category":"type"},{"location":"api/#CancellationTokens.CancellationTokenRegistration","page":"API Reference","title":"CancellationTokens.CancellationTokenRegistration","text":"CancellationTokenRegistration\n\nA handle returned by register that can be used to deregister the callback via close(registration).  Closing is idempotent and thread-safe.\n\n\n\n\n\n","category":"type"},{"location":"api/#CancellationTokens.OperationCanceledException","page":"API Reference","title":"CancellationTokens.OperationCanceledException","text":"OperationCanceledException <: Exception\n\nThrown when a cancellable operation is interrupted because its CancellationToken was cancelled.\n\nRetrieve the token that triggered the exception with get_token.\n\nExamples\n\ntry\n    sleep(60.0, token)\ncatch ex::OperationCanceledException\n    @info \"Cancelled\" token=get_token(ex)\nend\n\n\n\n\n\n","category":"type"},{"location":"api/#CancellationTokens.cancel","page":"API Reference","title":"CancellationTokens.cancel","text":"cancel(src::CancellationTokenSource)\n\nSignal cancellation. All tasks waiting on tokens from src will be unblocked. Calling cancel more than once is a no-op (idempotent). Thread-safe.\n\nSee also is_cancellation_requested, get_token.\n\n\n\n\n\n","category":"function"},{"location":"api/#CancellationTokens.get_token","page":"API Reference","title":"CancellationTokens.get_token","text":"get_token(src::CancellationTokenSource) -> CancellationToken\n\nReturn a CancellationToken linked to src.  Multiple calls return independent token objects that all reflect the same cancellation state.\n\n\n\n\n\nget_token(ex::OperationCanceledException) -> CancellationToken\n\nReturn the CancellationToken that caused the exception.\n\n\n\n\n\n","category":"function"},{"location":"api/#CancellationTokens.is_cancellation_requested","page":"API Reference","title":"CancellationTokens.is_cancellation_requested","text":"is_cancellation_requested(token::CancellationToken) -> Bool\n\nReturn true if cancel has been called (or a timeout has expired). This is a non-blocking, lock-free check on Julia 1.7+.\n\nExamples\n\nsrc = CancellationTokenSource()\ntoken = get_token(src)\nis_cancellation_requested(token)  # false\ncancel(src)\nis_cancellation_requested(token)  # true\n\n\n\n\n\n","category":"function"},{"location":"api/#CancellationTokens.register","page":"API Reference","title":"CancellationTokens.register","text":"register(callback, token::CancellationToken) -> CancellationTokenRegistration\n\nRegister callback (a zero-argument callable) to be invoked synchronously when cancel is called on the token's source.  If the token is already cancelled, callback is invoked immediately before returning.\n\nReturns a CancellationTokenRegistration that can be closed to deregister the callback.\n\nCallbacks are invoked synchronously during cancel(), so they should be non-blocking and fast — similar to .NET's CancellationToken.Register(Action).\n\nExamples\n\nsrc = CancellationTokenSource()\nreg = register(get_token(src)) do\n    @info \"Cancelled!\"\nend\ncancel(src)  # prints \"Cancelled!\"\n\n# Deregister before cancel — callback is NOT invoked\nsrc = CancellationTokenSource()\nreg = register(get_token(src)) do\n    error(\"should not run\")\nend\nclose(reg)\ncancel(src)  # nothing happens\n\n\n\n\n\n","category":"function"},{"location":"api/#Base.wait-Tuple{CancellationToken}","page":"API Reference","title":"Base.wait","text":"wait(token::CancellationToken)\n\nBlock the current task until the token's source is cancelled.  Returns immediately if already cancelled.\n\nThis is used internally by the cancellable overloads of sleep, take!, etc., but can also be called directly to build custom cancellable operations.\n\n\n\n\n\n","category":"method"},{"location":"api/#Base.close-Tuple{CancellationTokenSource}","page":"API Reference","title":"Base.close","text":"close(src::CancellationTokenSource)\n\nEquivalent to cancel(src). Provided so that CancellationTokenSource can be used with do-block resource patterns.\n\n\n\n\n\n","category":"method"},{"location":"api/#Base.close-Tuple{CancellationTokenRegistration}","page":"API Reference","title":"Base.close","text":"close(registration::CancellationTokenRegistration)\n\nDeregister a previously registered callback.  After this call the callback will not be invoked, even if the source is later cancelled.  No-op if the callback was already deregistered or if the source has already been cancelled.\n\n\n\n\n\n","category":"method"},{"location":"base/#Base-Function-Extensions","page":"Base Function Extensions","title":"Base Function Extensions","text":"CancellationTokens.jl extends several Base functions with an additional CancellationToken argument. These are new method signatures (different arity), so they cause zero method invalidations.","category":"section"},{"location":"base/#sleep","page":"Base Function Extensions","title":"sleep","text":"sleep(seconds, token)","category":"section"},{"location":"base/#wait(::Channel)","page":"Base Function Extensions","title":"wait(::Channel)","text":"wait(channel, token)","category":"section"},{"location":"base/#take!(::Channel)","page":"Base Function Extensions","title":"take!(::Channel)","text":"take!(channel, token)","category":"section"},{"location":"base/#readline-(sockets)","page":"Base Function Extensions","title":"readline (sockets)","text":"readline(socket, token; keep=false)","category":"section"},{"location":"base/#read-(sockets)","page":"Base Function Extensions","title":"read (sockets)","text":"read(socket, nb, token)","category":"section"},{"location":"base/#Base.sleep-Tuple{Real, CancellationToken}","page":"Base Function Extensions","title":"Base.sleep","text":"sleep(seconds::Real, token::CancellationToken)\n\nSleep for seconds, but wake up early with an OperationCanceledException if token is cancelled.\n\nExamples\n\nsrc = CancellationTokenSource()\n@async begin sleep(1); cancel(src) end\nsleep(60.0, get_token(src))  # throws OperationCanceledException after ~1 s\n\n\n\n\n\n","category":"method"},{"location":"base/#Base.wait-Tuple{Channel, CancellationToken}","page":"Base Function Extensions","title":"Base.wait","text":"wait(c::Channel, token::CancellationToken)\n\nWait for c to have data available, but throw OperationCanceledException if token is cancelled first.\n\nThe channel remains usable after cancellation.\n\nExamples\n\nch = Channel{Int}(1)\nsrc = CancellationTokenSource(5.0)    # 5 s timeout\nwait(ch, get_token(src))              # throws after 5 s if no data\n\n\n\n\n\n","category":"method"},{"location":"base/#Base.take!-Tuple{Channel, CancellationToken}","page":"Base Function Extensions","title":"Base.take!","text":"take!(c::Channel, token::CancellationToken)\n\nRemove and return a value from c, but throw OperationCanceledException if token is cancelled while waiting for data.\n\nThe channel remains usable after cancellation. Only buffered channels are supported; unbuffered (size-0) channels will raise an error.\n\nExamples\n\nch = Channel{Int}(10)\nsrc = CancellationTokenSource()\n@async begin sleep(1); put!(ch, 42) end\ntake!(ch, get_token(src))  # returns 42\n\n\n\n\n\n","category":"method"},{"location":"base/#Base.readline-Tuple{Union{Base.PipeEndpoint, Sockets.TCPSocket}, CancellationToken}","page":"Base Function Extensions","title":"Base.readline","text":"readline(socket::Union{Sockets.PipeEndpoint, Sockets.TCPSocket},\n         token::CancellationToken; keep=false)\n\nRead a line from socket, but throw OperationCanceledException if token is cancelled before data arrives.\n\nwarning: Cancellation closes the socket\nWhen token is cancelled, the underlying socket is closed to unblock the read.  This means the socket is no longer usable after cancellation.This is the only safe way to interrupt a socket read without corrupting other tasks that may be waiting on the same socket condition variable. Closing the socket ensures all readers receive a clean I/O error rather than having a foreign OperationCanceledException injected into unrelated tasks.For most timeout use cases this is the desired behaviour — if a read timed out, the protocol-level state is typically indeterminate anyway and the connection should be re-established.\n\nExamples\n\nsrc = CancellationTokenSource(5.0)  # 5 s timeout\ntry\n    line = readline(socket, get_token(src))\ncatch ex\n    if ex isa OperationCanceledException\n        # socket has been closed; reconnect if needed\n    end\nend\n\n\n\n\n\n","category":"method"},{"location":"base/#Base.read-Tuple{Union{Base.PipeEndpoint, Sockets.TCPSocket}, Integer, CancellationToken}","page":"Base Function Extensions","title":"Base.read","text":"read(socket::Union{Sockets.PipeEndpoint, Sockets.TCPSocket},\n     nb::Integer, token::CancellationToken)\n\nRead nb bytes from socket, but throw OperationCanceledException if token is cancelled before enough data arrives.\n\nwarning: Cancellation closes the socket\nWhen token is cancelled, the underlying socket is closed to unblock the read.  This means the socket is no longer usable after cancellation.This is the only safe way to interrupt a socket read without corrupting other tasks that may be waiting on the same socket condition variable. Closing the socket ensures all readers receive a clean I/O error rather than having a foreign OperationCanceledException injected into unrelated tasks.For most timeout use cases this is the desired behaviour — if a read timed out, the protocol-level state is typically indeterminate anyway and the connection should be re-established.\n\nExamples\n\nsrc = CancellationTokenSource(5.0)  # 5 s timeout\ntry\n    data = read(socket, 1024, get_token(src))\ncatch ex\n    if ex isa OperationCanceledException\n        # socket has been closed; reconnect if needed\n    end\nend\n\n\n\n\n\n","category":"method"},{"location":"#CancellationTokens.jl","page":"Home","title":"CancellationTokens.jl","text":"A Julia implementation of .NET's cancellation framework for cooperative cancellation of asynchronous and long-running operations.","category":"section"},{"location":"#Overview","page":"Home","title":"Overview","text":"The package follows .NET's design:\n\nCreate a CancellationTokenSource.\nObtain a CancellationToken with get_token.\nPass the token to cancellable operations as the last positional argument.\nOptionally register callbacks to run synchronously on cancellation.\nCall cancel on the source when the operation should stop.\nOperations throw an OperationCanceledException on cancellation.\n\nusing CancellationTokens\n\nsrc = CancellationTokenSource()\ntoken = get_token(src)\n\n# Start a long-running operation in a task\nt = @async begin\n    try\n        sleep(60.0, token)   # cancellable sleep\n        println(\"Completed\")\n    catch ex\n        if ex isa OperationCanceledException\n            println(\"Cancelled!\")\n        end\n    end\nend\n\nsleep(1.0)\ncancel(src)   # the task prints \"Cancelled!\"","category":"section"},{"location":"#Thread-Safety","page":"Home","title":"Thread Safety","text":"CancellationTokens.jl is thread-safe on all supported Julia versions:\n\nJulia 1.7+: Lock-free atomic operations (@atomic, @atomicreplace) matching .NET's Interlocked.CompareExchange and volatile patterns.\nJulia < 1.7: Falls back to ReentrantLock for safe concurrent access.\n\ncancel, is_cancellation_requested, wait, and all cancellable Base function extensions are safe to call from any thread.","category":"section"},{"location":"#Supported-Julia-Versions","page":"Home","title":"Supported Julia Versions","text":"The package supports Julia 1.0 and later, using version-specific optimisations behind @static if VERSION checks where beneficial.","category":"section"},{"location":"#CancellationTokens","page":"Home","title":"CancellationTokens","text":"CancellationTokens\n\nA Julia implementation of .NET's cancellation framework for cooperative cancellation of asynchronous and long-running operations.\n\nThe package provides CancellationTokenSource objects that produce CancellationToken values. Tokens are passed to cancellable operations (e.g. sleep, take!, wait) as the last positional argument. When cancel is called on the source, all operations holding a token from that source are notified and throw an OperationCanceledException.\n\nThread-safe on all Julia versions: Julia 1.7+ uses lock-free atomic operations matching .NET's Interlocked.CompareExchange pattern; older versions fall back to ReentrantLock.\n\nExports\n\nCancellationTokenSource\nCancellationToken\ncancel\nget_token\nis_cancellation_requested\nOperationCanceledException\n\n\n\n\n\n","category":"module"}]
}
